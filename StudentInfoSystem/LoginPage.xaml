<Page x:Class="StudentInfoSystem.LoginPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StudentInfoSystem"
        mc:Ignorable="d"
        Title="Student Information System" Height="450" Width="625">
    <GroupBox Header="Име и парола" Margin="51,70,51,225">
        <Grid Name="StudentInfoGrid">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Label Grid.Row="0" Grid.Column="0" Content="Потребителско име:"/>
            <TextBox Grid.Row="0" Grid.Column="1" Margin="0,0,0,5" Name="usernameTxt" />
            <Label Grid.Row="1" Grid.Column="0" Content="Парола:"/>
            <TextBox Grid.Row="1" Grid.Column="1" Margin="0,0,0,5" Name="passwordTxt" />
            
            <Button x:Name="loginButton" Content="LOGIN" HorizontalAlignment="Left"   Width="245" Grid.Column="1" Margin="0,40,0,-60" Grid.Row="1" Click="loginButton_Click"/>
        </Grid>
    </GroupBox>
</Page>
